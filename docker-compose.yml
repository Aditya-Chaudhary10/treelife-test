services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data        # workspaces, caches and the embedding model persist across restarts
